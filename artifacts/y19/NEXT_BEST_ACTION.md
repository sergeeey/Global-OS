# NEXT_BEST_ACTION

**Campaign status:** `stop_reason=terminal_scientific_result`  
**Do not ask operator for another in-family Hx by default.**

## If a new session resumes Y19

1. Read `Y19-RESEARCH-PROGRAM.md` + `CURRENT_STATE.json`.
2. If still `terminal_goal_met=true` and no new operator goal → **do not invent H8 feature hunt**.
3. Only continue if operator expands scope (new family / analytic program) or
   `stop_reason` is cleared with a new discriminating question.

## Out of scope without permission

- Windows 96m preflight / literal 48h / M1.5 claim
