# Y19-H5 — Why does N predict long transients?

**Campaign step** under `Y19-RESEARCH-PROGRAM.md` (not an operator ticket).  
**Prior:** H4 → mostly N.

## Competing explanations

- **H5a** state-space / raw N (or log2 N) dominates  
- **H5b** basin/cycle probe statistics beat N (incl. N-matched)  
- **H5d** protocol artifact (alt τ ablates N association)

(H5c proxy-other deferred if H5a/b/d leave residual uncertainty.)

## Gates

1. Structural probes vs `n` and `log2_n` (MCID)  
2. N-matched: structural vs activity within each N  
3. Protocol ablation: under τ=20, does `n` still beat activity?

Probe ICs ≠ labeled IC (no label leak).
