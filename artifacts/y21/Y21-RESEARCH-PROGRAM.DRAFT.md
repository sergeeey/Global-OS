# Y21 Research Program — Agreed Direction (prereg not yet locked)

**Status:** AGREED PLAN after Y20 · arms NOT started · prereg NOT locked  
**Y20 lesson:** process discipline ≠ sealed science gain

## Frozen upstream status (do not rewrite)

```text
Y19  → first autonomous research-loop example
Y20  → controlled A/B; sealed science advantage = NULL
Y21  → next attribution / transfer test
LH   → deferred
M1.5 → NOT CLAIMED
```

```text
GOS process discipline        EVIDENCE YES (Y20 directional)
GOS sealed science gain       NOT SHOWN
Universal advantage           NOT SHOWN
```

**Do not:** retune Y20 to win · mutate Y20 scorer · start LH/M1.5 · post-hoc claim changes.

## Why Y21 exists

1. **Transfer:** different cognitive class than Y20 causal generator.  
2. **Attribution hygiene:** baseline runner must never have authored generator / seen GT / seen task design.  
3. Prefer generator authored by a **separate process/line** or taken from an external fixed suite.

## Chosen task class (locked intent)

**Algorithmic reverse-engineering** (not causal-graph; not document-evidence).

Rationale: tests general cognitive transfer without picking a class that specially favors
epistemic/provenance machinery (document synthesis would bias toward GOS).

Concrete shape (to be finalized in `Y21-PREREG.md` before any arm):

- Hidden discrete rule / finite transducer / combinatorial process  
- Observational I/O traces only in public pack  
- Sealed holdout: predict outputs / recover rule components  
- Scorer + terminal criteria frozen before Arm A starts  

## What counts as GOS “win” (prereg before run)

| Priority | Metric |
|----------|--------|
| **Primary** | Sealed task performance |
| **Secondary** | human interventions · unsupported claims · recovery/state loss · process-trace completeness · cost/wall time |

Honest profiles allowed:

- same science, richer trace, higher cost → value profile, not “defeat”
- better science under equal caps → architecture contribution candidate
- worse science + higher cost → complexity not earned on this class

## A/B protocol (same as Y20 discipline)

```text
lock Y21 prereg + sealed generator (author ≠ arm runners)
→ Arm A strong baseline (never saw generator)
→ freeze A
→ Arm B same model + GOS loop (never saw generator)
→ freeze B
→ unseal → blind score → COMPARISON_REPORT → claims
```

Equal: model, caps, wall, tools, observable problem package only.

## Immediate next engineering steps

1. Author Y21 sealed generator in an **isolated** authorship path (no arm execution in that session).  
2. Commit `Y21-PREREG.md` + public pack hash + scorer → `prereg_boundary_sha`.  
3. Only then run Arm A / Arm B with fresh runners that did not author the generator.

## Non-goals

Y20 retuning · Ablation-C before Y21 lock · LH · M1.5 · Continual SI claim
