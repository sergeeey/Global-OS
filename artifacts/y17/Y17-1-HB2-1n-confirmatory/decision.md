# Decision — Y17-1 (H-B2-1n confirmatory replication)

## Verdict

**SUPPORTED** (Y-17 letter: **CONFIRMED**)

Primary Fisher-combined p = `8.074518e-10`  
Pre-registered α = `0.05` · Seeds = `400–459` · N_DIM = `{16,24,32,40,50}`

## Scope

Primary SUPPORTED (Fisher p≈8.07e-10). Secondary: 4/5 slices significant, all positive; N=50 individually null (p≈0.335) — scope caveat similar to H-B2-1o large-N weakness, walled off from flipping primary binary criterion.

## What this does NOT mean

- Does not rewrite H-B2-1n (still WEAKENED on its original majority-of-slices bar).
- Does not replace H-B2-1o; this is an independent Global OS replication on new seeds.
- Does not claim mechanistic explanation at N≥50 (N=50 slice individually null).
- Independent LLM/provider verification was **BLOCKED_ENVIRONMENT** (no cloud keys).

## Evidence pointers

- `experiments/metrics/run.json` (raw)
- `experiments/metrics/run_summary.json`
- `null_results.json` (N=50)
- `verification.json`
