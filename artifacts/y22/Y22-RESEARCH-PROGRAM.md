# Y22 Research Program — Reliability / Provenance Stress Test

**Status:** Binding once `CURRENT_STATE.prereg_locked=true`  
**Class:** Long evidence-synthesis with retractions, contradictions, delayed invalidation  
**Label:** Mechanism probe (epistemic/stateful) — **not** a neutral third exact-match rescue of Y20/Y21

## Frozen upstream

```text
Y19  → autonomous research-loop example
Y20  → sealed science gain = NULL (causal)
Y21  → sealed science gain = NULL (algorithmic)
Y20/Y21 scorers → FROZEN FOREVER
LH / M1.5 → deferred / NOT CLAIMED
```

## Mechanism hypothesis (preregistered)

> If Global OS advantage is not raw reasoning amplification but
> continuity / accountability / provenance / recovery / epistemic discipline,
> then it should appear more clearly on tasks with dynamically changing evidence
> and delayed corrections than on short sealed exact-match problems.

Competing:

| ID | Statement |
|----|-----------|
| H_reliability_multiplier | B wins frozen reliability composite by MCID under equal caps |
| H_still_null | A matches/beats B on composite — process beauty still unpaid |
| H_inconclusive | Within MCID |

## Primary (frozen before arms)

**Composite** `reliability_composite` with fixed weights:

| Component | Weight |
|-----------|--------|
| final_factual_correctness | 0.35 |
| invalidated_claim_cleanup (F1) | 0.25 |
| unsupported_claims_score | 0.20 |
| recovery_fidelity | 0.20 |

B primary win iff `composite_B >= composite_A + 0.05`.  
Process secondaries **cannot override** primary.

## Arms / sequence

Same as Y20/Y21: equal model/data/caps; A strong baseline; B + GOS loop;  
A→freeze→B→freeze→unseal→score→COMPARISON_REPORT.

## Hard forbids

- Retune Y20/Y21 scorers or replay arms  
- Change Y22 weights/MCID after arm start  
- Claim universal advantage from Y22 alone  
- LH / M1.5 / Continual SI from Y22 alone  
- Disguise this as “neutral third class” — it is a **mechanism** test
